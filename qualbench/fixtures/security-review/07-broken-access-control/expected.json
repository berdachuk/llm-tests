{
  "required_pattern_groups": [
    ["broken access control", "insecure direct object reference", "\\bIDOR\\b", "authorization"],
    ["belongs to.*(user|customer)", "owns? the invoice", "ownership check", "verify.*owner", "check.*owner", "current(ly)? authenticated user"]
  ]
}
