Task 06: The user's request is missing a required piece of information
that the tool schema demands (no default is sensible). The model should
NOT guess/hallucinate a value and call the tool anyway -- it should ask
for the missing information, or at minimum not fabricate the missing
required argument.
