Task 03: The user describes a monetary amount and a percentage in prose;
the model must call the tool with correctly-typed numeric arguments
(not strings), including converting a percentage phrase into a decimal
fraction as the schema requires.
