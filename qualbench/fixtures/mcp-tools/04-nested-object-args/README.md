Task 04: The tool's schema requires a nested object argument (an
`address` object with sub-fields). The model must correctly structure
the nested JSON rather than flattening it or putting the whole address
in a single string field.
