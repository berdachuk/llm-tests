Task 07: The conversation already contains a completed tool call and its
result (a lookup that returned a specific ID). The model's next action
must correctly reuse that ID from the tool result rather than
re-deriving, guessing, or ignoring it.
