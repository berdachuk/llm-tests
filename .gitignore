.venv/
__pycache__/
*.pyc
.pytest_cache/
node_modules/
target/

# Scratch SQL migration candidates written by qualbench/fixtures/sql-migrations/check.py
up.candidate.sql

# Local environment overrides (server URL/model/etc.) -- see .env.sample.
.env

# Plaintext SSH/sudo passwords for remote test targets -- never commit.
secrets/
